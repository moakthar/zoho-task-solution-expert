name: Hello World Workflow

on: push

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Print Hello World
        run: |
          echo "Hello World"
          echo "Welcome to GitHub Actions"
          date

